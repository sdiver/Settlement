<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration 
PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-config.dtd">
<!-- author Shixiangren
	 since 2016.2.14
	 配置mybatis -->
<configuration>
        <!-- 命名空间 -->
    <typeAliases>
   		 <typeAlias alias="user_info" type="model.user_info"/>
         <typeAlias alias="authorityVo" type="model.authorityVo"/>
    </typeAliases>
    <!-- 映射map -->
    <!-- 映射map -->
    <mappers>
    </mappers>
</configuration>